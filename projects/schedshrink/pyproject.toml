[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "schedshrink"
version = "0.1.0"
description = "Deterministic exploration of cooperative task interleavings"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Alexandre Mosiashvili"}]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6", "mypy>=1.11"]

[project.scripts]
interleaving-lab = "schedshrink.cli:main"
schedshrink = "schedshrink.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/schedshrink"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.mypy]
strict = true
